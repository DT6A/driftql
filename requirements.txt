ogbench == 1.1.0
jax >= 0.6.2
flax >= 0.10.7
distrax >= 0.1.5
ml_collections
matplotlib
moviepy
wandb
d4rl
gymnasium == 0.29.1  # We can't use gymnasium >= 1.0.0 because it breaks d4rl.
gym == 0.23.1  # For d4rl.
numpy == 1.26.4  # For d4rl.
Shimmy==0.2.1  # For d4rl.
Cython < 3  # For d4rl.
